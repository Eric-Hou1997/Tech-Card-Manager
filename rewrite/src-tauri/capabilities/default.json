{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Validation window only. No arbitrary filesystem, shell, updater or remote-window grants.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
