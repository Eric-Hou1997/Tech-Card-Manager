# 第 5–8 步迁移执行记录

状态：**部分实现，未达到四步整体完成条件，不可替代正式 v4.1.0。** 开发位于 `v5.0.0-rewrite`；本轮没有提交、推送、打 tag 或发布。旧平台代码和正式业务引擎仍保留。

## 已实现的基础和读取链

- `rewrite/src-tauri/core/` 为独立可测试 Rust 核心。Vue → Tauri 命令/事件 → Store/读取用例 → XML/路径/SQLite 适配；两仓库独立构建，不运行时互相引用。
- Rust 生成 TypeScript 类型；配置 revision CAS、数据库版本拒绝降级、单一数据库所有者、持久化操作结果与跨操作种类 ID 冲突检查。
- 扫描任务明确根目录范围和 Movie/TV 空间，冻结启动语言；持久化进度、历史、暂停、恢复、取消、退出中断。恢复从根重新遍历并复用未改变的索引，不是旧引擎游标直接续跑。
- 只读 NFO 解析、根标签权威归属、Technical Specs、同 IMDb 不同路径条目的独立身份。保存 source hash/parser revision，增量刷新；异常路径可定位，离线根保留已有索引。
- Vue 工作台已有根目录选择、显式范围、搜索、异常过滤、分页、选择、Inspector、文件管理器定位、任务状态与控制。Movie/TV 的窗口内搜索和选择各自保留。
- 凭据只有 Rust 接口，原生 keyring 适配；前端没有获取凭据的通用命令。已定义 8 语言注册表、任务语言快照，完整界面翻译和语言包流程尚未接入。
- 本地窗口权限与 CSP 保持隔离，外部网页没有管理命令；工作线程/事件监听/计时器分别持有并清理。退出时等待扫描线程停止。

## 可重复验证及证据边界

- 核心回归覆盖只读字节和 mtime、重复 IMDb、Movie/TV 范围、中文长路径、大写扩展名、增量改变、坏 XML/DTD、符号链接拒绝、离线根、配置冲突、操作 ID 重试/查询、任务取消/恢复、重启历史和独占数据库。
- 两产品均通过本机 Rust 核心测试、类型生成漂移、Vue TypeScript/Vite 构建、Rust fmt/Clippy。准确测试数及源哈希见 [本轮自动检查](evidence/phase58-checks.json)。
- 本机原生 macOS ARM64 DMG 的隔离安装、窗口 IPC 与退出另见 [本轮原生烟测](evidence/phase58-native-smoke.json)。此前 `native-smoke.json` 是步骤 4 的历史构建，不代表当前源码。
- 原生界面使用仓库真实 NFO 的隔离副本：两个相同 IMDb 文件和一个损坏 XML，均显示为独立条目；坏文件使扫描报告 failed/3 条/1 错。ITM 实测 Inspector、中文搜索、空间切换保留状态及凭据往返后删除；TCM 实测异常过滤和 Finder 选中对应 NFO。两应用独立数据目录、退出成功。
- 上述界面检查使用测试副本，并未对维护者真实媒体库完成验收；GUI 前没有记录所有文件的纳秒级 mtime，不能将单元测试的 mtime 证明扩大为这次 GUI 的完整时间属性证据。
- 5 目标 CI 已加入核心测试、类型漂移和构建检查，但尚未推送/执行。Windows/Linux（含 ARM64）仍为 unverified，不能声称五目标持续通过。

## 必须继续完成的工作

| 步骤 | 尚未通过的完成条件 |
| --- | --- |
| 5 | 旧配置/数据库完整迁移、完整操作日志和诊断导出、凭据实际业务使用、完整 UI 多语言/外部语言包、目标平台适配与真实运行验收。当前 SQLite 只建立初始 schema，不能等同于所有旧数据迁移完成。 |
| 6 | 根目录自动发现/路径映射、TV 季集层级与季选择、完整筛选和状态语义、窗口重启后的 UI 状态、真实媒体库新旧逐项差分、网络共享/大小写/权限及大库性能验收。当前目录查询在内存过滤，需大库基准后优化。 |
| 7 | ITM 实际 IMDb 获取、HTML/WebView 后备、缓存/补全/自动与批量流程、完整 metadata 和归属编辑、预演审批与写入界面、各平台事务/权限/崩溃恢复及真实样本。TCM 不实现媒体写入。 |
| 8 | ITM AI 实际 HTTP 适配、完整配置 UI、持久化费用账本与缓存、失败重试编排、审批/重建/选中项批处理和完整任务迁移。只有所有台账行为通过后才能退出旧引擎。 |

## 显式差异与限制

- 解析超过 32 MiB 的 NFO、DTD、符号链接/Windows reparse 路径时失败并定位，不擅自修复或绕过；与旧版的全部边界差异尚需逐项对照。
- ITM 归属比较使用 Python 对应的 Unicode full casefold；TCM 保留其原版小写比较语义。ITM 合并已知 Specs 重复 section，TCM 保留最后 section；未将两个产品不同旧行为强行统一。
- 扫描取消在读取检查点生效；网络文件系统阻塞读、权限与锁行为必须真实平台验证。当前单元测试不证明任意网络故障都能即时退出。
- 第 4 步记录的 IMDb HTTP 202、旧 OTA 签名兼容、Emby 权限和更新/卸载验收阻塞继续有效，首发矩阵没有缩减。
- 未迁移功能均仍在 [完整功能台账](features.json)；`partial` 只代表列出的实现片段，不表示整个功能组或原版入口验收通过。

## TCM 产品边界

TCM 核心不包含 ITM 的 AI、规则生成、Specs 写入或事务模块。现阶段只迁移 Manager 的基础媒体读取；Emby 资源发布、Web Card 浏览器加载与渲染、旧组件维护、更新/修复/禁用/移除和回滚仍由后续对应阶段实现。TCM 的只读测试不会证明这些服务链已经迁移。

- [维护者模拟 NFO 库：101 份样本验收](08-sample-library.md)
