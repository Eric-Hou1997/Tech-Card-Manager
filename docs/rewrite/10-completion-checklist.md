# 第 9–12 步逐项实施与验收

本清单重新按维护者指令建立。每行必须有对应实现和证据才可勾选；构建、安装烟测、静态测试不替代完整业务和桌面验收。此前 18 个包是验证包。

## 第 9 步：TCM Emby 集成和生命周期

- [ ] 只读 NFO → 索引 → 卡片数据 → 资源发布 → Emby 加载 → 实际卡片展示 → 服务停止与清理完整通过。
- [ ] Emby 环境发现、版本检查、安装目录与数据目录配置、路径映射。
- [ ] 安装、更新、修复、禁用、移除与重复操作。
- [ ] 历史组件识别、可审查维护计划、明确确认、权限申请与失败恢复。
- [ ] 外部备份、事务提交、并发修改保护、中断回滚。
- [ ] Web Card 数据与八种语言发布；Manager 与卡片发布状态分别显示。
- [ ] 服务启停、后台运行、租约失效、异常退出和正常退出清理。
- [ ] Emby 升级后自动重新检查与可操作修复。
- [ ] macOS/Linux 独立路径与权限适配。
- [ ] Windows ARM64 Manager 与真实 Emby 安装组合验证。

## 第 10 步：完整界面与系统行为

- [ ] 两产品每个原版页面、按钮、菜单、设置、快捷入口都有对应实现。
- [ ] 表格排序、搜索、筛选、选中项、布局状态持久化。
- [ ] Movie/TV 独立状态、TV 层级、季选择、批量范围准确。
- [ ] 长任务真实进度；错误可回到受影响项目。
- [ ] 最小化、关闭窗口、后台运行、完全退出、二次启动与登录启动。
- [ ] 原生菜单、托盘、对话框、权限和错误提示语言一致。
- [ ] 三种内置语言与五种外部语言包；切换不丢失界面或任务状态。
- [ ] 八种语言、窄窗口、缩放、键盘与无障碍操作验收。
- [ ] Linux 缺少托盘等能力时有等效入口，并记录真实平台差异。

## 第 11 步：数据迁移与 OTA

- [ ] 旧配置、媒体库、路径映射、提示词、凭据、索引、缓存、备份、任务历史和启动设置逐字段迁移。
- [ ] 迁移可重复执行、中断恢复、回退；旧数据保留且不可静默丢弃。
- [ ] 产品、系统、应用架构、安装渠道精确选择更新产物。
- [ ] macOS ARM64 DMG 初装和应用内更新。
- [ ] Windows x64/ARM64 一次点击下载、验签、安装、重启及结果验证。
- [ ] Linux x64/ARM64 AppImage 应用内更新。
- [ ] Linux x64/ARM64 DEB/RPM 签名软件源及包管理器升级方案。
- [ ] Windows ARM 机器上的旧 x64 安装保持应用架构，避免重复安装和数据目录变化。
- [ ] 应用替换失败与数据迁移失败分别恢复；没有无法判断的半安装状态。
- [ ] 旧 OTA 签名与新格式兼容验证；既有密钥不自动更换。

## 第 12 步：最终验收与本地交付

- [ ] 功能表：原版功能全有实现与验证，无未解释遗漏；补齐第 5–8 步尚未接通的 ITM 获取、写入、AI 和审批链。
- [ ] 缺陷表：已知新增缺陷清零，每项修复有回归证据。
- [ ] 平台表：10 个应用目标、18 个安装包均通过实际安装、升级、卸载和桌面验收。
- [ ] 迁移表：旧数据、更新、失败恢复、ITM→TCM 衔接通过。
- [ ] 扫描、批处理、缓存、后台常驻长时间稳定性。
- [ ] 安装、升级、卸载、二次启动、异常退出与恢复。
- [ ] 新旧 ITM/TCM 数据兼容组合验证。
- [ ] 检查实际运行产物；完成替代后退出旧 Go/Python/PowerShell 业务引擎。
- [ ] 八种语言 README、隐私政策、条款、安装更新说明完整同步。
- [ ] 对最终交付提交重新验证并生成本地安装包、更新产物、签名、校验清单和语言包。
- [ ] 所有验收通过后才处理旧实现与主线替换；当前只同步重写分支，不推 main、不建新 tag、不发布 GitHub Release。

## 当前实施记录

TCM 新增 Rust `emby` / `card_service` 模块及 Tauri 维护界面，包含公开 schema 7 索引、计划确认、备份、事务回滚、安装/修复/移除和服务租约。卡片渲染资源从 v4.1.0 基线独立保留；不调用旧 PowerShell 引擎。新增 9 项回归及原有 14 项读取回归在本机通过，桌面编译和前端构建通过。真实 Emby 页面云端验收正在接入；本记录不勾选整项完成。

### 本轮增量证据

- 两产品新增旧数据快照计划、SQLite schema 2 原子导入与桌面确认入口。每产品 5 项迁移回归通过：原始日志/自定义提示词保留、重复请求、最后一个文件改变时整体回滚、操作 ID 冲突、凭据边界、离线/禁用根及畸形 JSON 保留。导入原字节和合并可用目录不等于所有旧业务适配完成，回执明确记录待核对状态。
- TCM 在真实 Emby 4.9.5.0 的 Linux x64/ARM64 服务器上通过 Rust 读取/发布 → 实际客户端展示 → 停止隐藏 → 重启显示 → 移除恢复链；NFO 字节和纳秒级 mtime 保持。云端运行 34564875724，源提交 49db938。Windows/macOS 组合继续验收。

### 真实 Emby 五目标增量

TCM 源提交 a435dcc：Linux x64/ARM64 运行 34567673874，Windows x64/ARM64 与 macOS ARM64 运行 34567673854 全部通过。实际链路使用官方 Emby 4.9.5.0、真实 NFO/视频、正式 Rust Store/Integration/CardService 及官方 Web 客户端，验证显示、停止隐藏、恢复、移除、服务重启及清理。Windows ARM64 是原生 Rust 管理核心加 x64 Emby 仿真组合；Windows x64 节点为 Server 2022。此证据不替代 Tauri 管理界面的完整交互和所有发行版验收。

新增实现正在继续验收：Emby 发现、安装/数据目录持久化、服务器版本检查、旧卡片显式迁移、八种卡片语言和语言切换。未完成的整项仍不勾选。

### OTA 增量与明确边界

ITM 已用原有 Ed25519 私钥验证 Tauri Minisign 格式签名；原密钥和公钥不变，篡改产物拒绝。新增精确目标选择、下载/校验进度、数据库快照、macOS 原子目录交换、AppImage 原子替换、旧应用保留和恢复。新增正常写入冻结验证。TCM 的签名信任根仍待维护者确定，更新插件未启用；不得复用 ITM 密钥。真实跨版本升级、完整数据失败恢复、软件源及旧客户端桥接尚未验收，因此第 11 步保持未完成。

### 界面与生命周期增量

新增 Movie/TV 独立视图状态与操作 ID 保存、目录归属 TV 层级和整季选择、后台关闭策略及退出清理、登录启动事务和中断恢复。登录项适配保持 Unicode 路径及 XML/桌面启动参数转义；歧义节目归属和符号链接覆盖具有回归。核心测试与本机严格编译通过。ITM 的共享界面已完成本机搜索、跨空间选择、整季选择和重启恢复的实际操作；TCM 自身的完整桌面操作和跨平台生命周期仍须分别验收。

### Emby 根目录发现与混合媒体范围（2026-09-11）

- 使用只读 SQLite 连接读取 Emby 4.9.5.0 的 library.db，识别虚拟包装层下的物理根目录；查询有超时、行数和 NFO 探测上限，异常与不完整证据明确展示。
- Windows 盘符/UNC 与本机路径采用组件边界和最长前缀映射；映射设置具备持久化、版本冲突和操作重放保护。Manager 新增发现、映射和明确加入 Movie/TV 操作。
- 混合目录可以分别加入 Movie 和 TV，不移动文件；扫描按真实 NFO 类型分流，损坏文件在两个范围各自保留可定位异常。旧 mixed 根目录迁移为两个范围。
- 卡片脚本 URL 使用实际内容哈希，避免更新后继续使用旧缓存。
- 本地完整 Rust 工作区测试、严格 clippy、TypeScript/Vue 构建通过；新增只读数据库与映射 3 项、混合读取/迁移回归通过。真实 Emby 五个目标新增数据库发现验收仍待云端结果。
- 同步 Linux 登录转义、数据库锁退出释放与损坏 JSON 凭据保护。GIO 对百分号可执行路径的检查改为通过系统 /usr/bin/env 直接 exec 应用，等待 Linux 真实验证。
- 特权安装目录权限、完整 UI 多语言、旧数据业务适配及 OTA 等剩余项仍未放行；不把本轮通过等同于第 9–12 步完成。

### 常驻索引发布与续租分离（2026-09-11）

- 卡片心跳与索引发布采用两个明确所有者，磁盘校验与全库构造不持有 UI 状态锁。停止时等待发布结束，再停止并等待续租，最后明确禁用运行租约。失败仍保留结构化原因。
- 数据库升级至内部 schema 3，以与索引同一事务的修订号识别变化；无变化扫描不反复反序列化全库，暂停/中断/正在进行的扫描不发布部分索引。
- 本地新增修订持久化、无变化跳过、暂停恢复、真实发布文件变化、退出后无续写及 NFO 字节/时间不变回归；原有读取、迁移、Emby 生命周期与严格 clippy 通过。真实 Emby 驱动已改为使用与桌面相同的 Store 联动服务，等待对应云端回归。
- 上一提交 9335a34 的真实 Emby 数据库发现与八语言卡片展示已经五个目标通过：Linux 34578519689，macOS/Windows 34578519522。该证据不代替特权安装目录和 Manager 完整桌面验收。

### Windows ARM 服务停止回归（2026-09-11）

- 新 Store 联动服务在 Linux 双架构、macOS 和 Windows x64 的真实 Emby 回归通过；Windows ARM 在八语言展示之后，停止时遇到 Emby 文件替换 Access denied（34581478644），本轮不得按五目标通过放行。
- 文件替换对短暂共享/访问失败采用约 1.3 秒上限的重试，每次重新校验目标哈希；只读权限和外部内容变化仍明确失败。补充 Windows 不允许删除共享的读取句柄、重试期间外部改动两个回归。所有写入错误携带路径。
- 云端 Rust 驱动退出将立即返回实际错误，避免被响应超时覆盖；隔离环境删除等待短暂 Windows 文件占用，并独立记录清理失败，不覆盖最初的业务错误。修复仍待 Windows 实际回归结果。

### 后台发布与真实 Emby 五目标复验（2026-09-11）

源提交 `8c14e3e`：五目标构建/安装运行检查（34583097498）全部通过。真实 Emby 工作流 34583097440、34583097465 同样全部通过，包含数据库物理根发现、索引和资源字节一致、实际卡片显示、八语言切换、停止隐藏、重启显示、移除恢复及 NFO 字节/mtime 不变，详见 [归档证据](evidence/emby-8c14e3e.json)。

后台续租与数据发布已拆分为独立拥有、退出时汇合的工作线程；索引事务修订号驱动发布，暂停/中断扫描不发布局部快照。Windows ARM 实测停止阶段的瞬时文件占用已修复为有限重试，每次重试验证原文件哈希；回归覆盖占用解除和外部修改保护。上述结果不关闭安装目录提权、完整 Manager 界面、迁移、OTA 与最终放行的未完成项。

### 已加载媒体页面与原生视频信息并排验收（2026-09-11）

源提交 `1742f61` 的真实 Emby 五目标验收通过：Linux 双架构运行 34591347204，macOS ARM64 和 Windows 双架构运行 34591347145。验收现等待 Emby 实际探测到 H264 视频流、媒体详情完成加载，并确认 Technical Specs 卡片与原生视频信息在同一媒体行；八语言切换、停止隐藏、重启显示、移除及 NFO 字节/mtime 保持也通过。Linux 隔离实例使用官方包自带 ffdetect/ffmpeg/ffprobe 与对应库路径，避免把媒体探测失败的半加载页面当作完整展示。见 [五目标归档证据](evidence/emby-loaded-1742f61.json)。

后续 `030c54d` 将续租测试从固定短等待改为有上限地等待实际续租序号变化，五目标构建/安装运行检查通过；产品行为没有因等待方式改变。系统保护目录提权、完整 Manager 界面、旧数据迁移、OTA 和最终交付继续保持独立门禁。


### 中断恢复核对与受保护目录预览（2026-09-12）

- 总体工程工作量粗估约 60%，第 9–12 步约 40%；这是实现与剩余工作量的人工估计，不是完整验收通过率。最终放行安装包仍为 0/18。完整语言、数据迁移、OTA、系统授权与最终桌面验收不因基础构建通过而关闭。
- ITM `3e5d3f7` 的五目标云端检查已重新查询确认全部成功（34634714324）。TCM 中断前的未提交改动重新审查；失效的进程回执不作为当前完成证据，已重新运行工作区 66 项测试、新增原生连接逻辑 2 项回归和 Vue/TypeScript 构建，均取得正常结束结果。没有启动或被锁屏阻止的原生操作继续列为未验收。
- Emby 目录权限不足时可以只读检查并生成原来的逐文件维护计划与可验证外部备份，状态明确为 `permission-required`。只读对象在核心层拒绝提交、恢复、数据发布和租约启停，不能只靠按钮禁用保证边界；私有备份增加独占锁，正常写入仍保留跨数据目录的 Emby 锁。
- 权限预检区分普通 I/O 失败与系统拒绝；即使留有可写的旧锁文件，也验证目录是否仍允许原子替换所需的临时文件。未完成的旧事务在只读模式显示需要恢复，不擅自执行。真正取得访问能力后重新打开，并在提交前复核已审查的全部源哈希。
- 三项新增 Emby 回归覆盖无目标目录/文件变化的计划预览、所有写入口拒绝、外部备份互斥、权限恢复后执行原计划、中断事务只读检查和外部升级冲突，以及 Unix 实际不可写目录。另两项桌面连接逻辑回归验证新目录失败保留原连接和锁、同目录权限复查后复用唯一连接。此测试不是 UAC、macOS 授权窗口或 Linux polkit 的实际验收。
- 恢复审查修正了新目录打开失败提前释放旧连接的问题，并从正常服务状态轮询移除新增的全量事务目录检查。系统授权 helper、权限范围收敛、受保护目录中的持续发布和跨平台安装/卸载仍须继续实施；本增量只完成授权前预览，不宣称提权链已完成。

### 维护回执与受限协议（2026-09-12）

- `0b45124` 的五目标构建/安装检查、Linux 与 Windows/macOS 真实 Emby 加载链全部成功；见 [五目标报告](evidence/emby-loaded-0b45124.json)。这些仍是隔离普通用户目录，不包含系统提权验收。补齐 Windows/macOS Emby 工作流对 `rewrite/**` 与语言包变更的自动触发。
- 新维护计划的确认指纹绑定目标、操作、逐文件前后哈希和可见清单。相同请求遇到外部升级后的不同文件会得到不同确认指纹；仍读取此前重写日志，重复已提交请求不重新写入。原生界面可按操作 ID 查询，重启恢复最后一项操作；未知结果只允许查询，不能盲目重发。
- 核心新增受限维护协议及客户端：请求不能携带任意路径、脚本或系统命令；帧大小受限，序号与结果类型匹配，重复/冲突请求区分。断线、损坏请求或回复发送失败均停止服务和续租。该模块尚未接入系统授权启动器、身份验证通道与受保护日志，不能宣称提权链已完成。
- 15 项 Emby 回归、8 项维护协议回归、完整工作区、严格 Clippy 和前端构建已取得正常结束结果。新增双向通道测试证明提交后丢失回复不会自动重试，重新连接可以读到已提交回执。
- 本机原生应用已逐项完成临时网页目录的计划确认、安装、查询、服务启动、退出清理、重启回执和移除；HTML 最终字节完全恢复，拥有的资源已移除。见 [原生维护回执验收](evidence/native-maintenance-receipt.json)。此验证不替代真实 Emby 页面或 UAC/polkit/macOS 授权。

### Linux 受保护目录权限链路增量（2026-09-12）

- 9bd4ef3 的五目标构建/安装运行（34685810280）和五种真实 Emby 组合（34685810270、34685810273）均已完成并通过；这不代表受保护安装目录或完整桌面验收通过。
- Linux 新增随包分发的独立 Rust 权限辅助程序，由 Manager 通过系统 pkexec 授权启动。只使用私有继承管道，不开放网络端口、不安装免授权策略、不传递任意命令或用户可编辑的恢复日志。辅助程序核对操作系统记录的父进程和身份，只管理选定的 root 所有、非组/全局可写目录及受限卡片文件；普通可写安装继续沿用已有路径。其他所有者或非受信任路径明确拒绝，仍需对应兼容适配。
- root 私有备份和回执固定在 `/var/lib/tech-card-manager-validation`；不导入普通用户日志。授权后新建维护计划并确认；已有系统维护操作保留 ID，断线/重启后重新授权查询，不能盲目重复写入。有时限的读写、后台索引发布、EOF 停止租约和进程退出分别管理。维护调用移到阻塞工作池，避免系统授权或文件操作阻塞界面线程。
- 本机执行了管道部分帧/写阻塞超时、目录/硬链接/符号链接拒绝、授权取消/拒绝分类及启动失败进程清理回归；完整工作区 84 项通过，严格 clippy 通过。前端构建及本机普通权限原生安装、服务启停、移除和完整 HTML 恢复也已通过，见 [原生异步维护复验](evidence/native-async-maintenance.json)。Linux 云端驱动尚待运行，不提前计为通过。
- 新云端测试使用隔离 root 目录和真正的辅助进程，覆盖普通用户写拒绝、安装、服务租约、EOF 清理、重启回执、丢失 Apply 回复、移除恢复与 NFO 字节/mtime 保持。它使用测试机管理员身份，不替代实际 polkit 弹窗、完整 Manager 桌面或真实 Emby 展示验收。
- macOS/Windows 的受信任权限启动器仍未接通；本机当前未发现有效代码签名身份，macOS 签名辅助程序安装验收保留阻塞。最终 18 包及整项步骤仍不勾选。

平台依据：[pkexec 授权与退出状态](https://polkit.pages.freedesktop.org/polkit/pkexec.1.html)、[Tauri 辅助程序打包](https://tauri.app/develop/sidecar/)。

Linux AppImage 的 FUSE 挂载与提权辅助程序可执行性需要独立验证；不能从 DEB/RPM 中的权限链路推断 AppImage 已通过。

首轮 d310c99 云端运行 34687707740 未通过：Windows lint 包含了 Unix 测试模块；Linux 原生 lint 早于辅助程序构建；macOS 完成编译后 DMG 工具失败。平台条件与构建顺序已修正，DMG 打包启用详细日志后重新验证；失败运行不计为完成。

### 云端辅助程序进程验收断点（2026-09-12）

- `a2d5b9d` 的 macOS 构建、DMG 安装启动及退出已通过；Linux x64/ARM64 三种包均构建完成，但受保护目录辅助程序进程测试失败，后续安装启动步骤未运行，不能记为通过。
- 失败为 `maintenance-untrusted-path`，发生于云端 `/opt` 下样本首次打开，原日志未打印结构化路径。测试样本移到与实际恢复日志相同的 `/var/lib` 保护父目录，并补充完整错误路径及 uid/mode/link 诊断。尚未证实具体失败祖先，不以推测写成已定位结论；仍保留全部目录所有权检查，不放宽授权边界。
- 维护者确认暂无 Apple Developer ID Application 证书，macOS 签名及受信任辅助程序安装验收继续阻塞，不阻止其他可执行开发测试，也不计为验收通过。
